# anacrontab: configuration file for anacron

# See anacron(8) and anacrontab(5) for details. Note: this doesn't start daemon.
# This is meant to run in .zprofile or similar.
# Timestamp set in anacron/spool/ when anacron job has run.

SHELL=/bin/zsh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin:$HOME/bin

# period  delay  job-identifier  command
7         15     zinit-update    zsh -ic 'command -v zinit &> /dev/null && zinit self-update -q && zinit update --all -q'
1         15     empty-tmp       rm -rf $HOME/tmp/*
